//#include <iostream>
//
//int main() {
    //Exersice 1
    /*double amount;

    std::cout << "Enter sum: ";
    std::cin >> amount;

    int hryvnia = amount; 
    double kopiyky_double = (amount - hryvnia) * 100; 
    int kopiyky = kopiyky_double + 0.5; 

    std::cout << hryvnia << " grn " << kopiyky << " kop." << std::endl;*/

    //Exersice 2
   /* int distance, minutes, seconds, total_seconds, speed_mps, speed_kmph;

    std::cout << "Enter the length of the distance (meters): ";
    std::cin >> distance;

    std::cout << "Enter the time (min sec): ";
    std::cin >> minutes >> seconds;

    total_seconds = minutes * 60 + seconds;

    speed_mps = distance / total_seconds;

    speed_kmph = speed_mps * 3600 / 1000;

    std::cout << "Distance: " << distance << " m;" << std::endl;
    std::cout << "Time: " << minutes << " хв " << seconds << " sec = " << total_seconds << " sec;" << std::endl;
    std::cout << "You ran fast " << speed_kmph << " km/h." << std::endl;*/

    //Exercise 3
    /*int total_days, weeks, remaining_days;

    std::cout << "Enter the number of days: ";
    std::cin >> total_days;

    weeks = total_days / 7;

    remaining_days = total_days % 7;

    std::cout << total_days << " days is " << weeks << " weeks and " << remaining_days << " days." << std::endl;*/

    //Exercise 4
   /* int hryvnia, kopiyky;

    std::cout << "Enter the amount of hryvnias: ";
    std::cin >> hryvnia;

    std::cout << "Enter the number of pennies: ";
    std::cin >> kopiyky;

    while (kopiyky >= 100) {
        hryvnia++;
        kopiyky -= 100;
    }

    std::cout << "Corrected amount: " << hryvnia << " grn " << kopiyky << " kop." << std::endl;*/


//    return 0;
//}
